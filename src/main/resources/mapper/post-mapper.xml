<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace= "com.example.bootproject.post.mapper.PostMapper">
    <resultMap  type="com.example.bootproject.post.domain.Post"  id="PostResultMap">
        <id property="id" column="id" />
        <result property="title" column="title" />
        <result property="content" column="content" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
        <result property="deletedAt" column="deleted_at" />
    </resultMap>
    <select id="selectAllPosts" resultMap="PostResultMap">
        select * from post
    </select>

    <select id="getPost" resultMap="PostResultMap">
        select * from post where id = #{id}
    </select>

    <insert id="insertPost" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO post (title, content, created_at, updated_at)
        VALUES (#{title}, #{content}, NOW(), NOW())
    </insert>

    <update id="updatePost">
        UPDATE post
        SET title = #{title},content = #{content},updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="softDeletePost">
        UPDATE post SET deleted_at = NOW() WHERE id = #{id}
    </update>

    <delete id="deletePost" >
        DELETE FROM post WHERE id = ${id}
    </delete>

</mapper>
